services:
  web:
    build: .
    container_name: highdesign_dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
